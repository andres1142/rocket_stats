import React from 'react';

export default function Header() {
    return (
        <header>
            <h1>Rocket Stats</h1>
            <p>This is a react component. We are going to be using different components to represent
            different graphs, tables or etc.</p>
        </header>
    )
}